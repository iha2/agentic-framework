{
  "name": "invisible-unicode-lint",
  "private": true,
  "type": "module",
  "description": "Deterministic invisible-Unicode / Trojan Source style scanner (no AI).",
  "scripts": {
    "build": "tsc",
    "lint:invisible-unicode": "node dist/lint-invisible-unicode.js",
    "test": "npm run build && node --test dist/lint-invisible-unicode.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
